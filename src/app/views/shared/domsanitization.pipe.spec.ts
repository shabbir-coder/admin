import { DomsanitizationPipe } from './domsanitization.pipe';

describe('DomsanitizationPipe', () => {
  it('create an instance', () => {
    const pipe = new DomsanitizationPipe();
    expect(pipe).toBeTruthy();
  });
});
